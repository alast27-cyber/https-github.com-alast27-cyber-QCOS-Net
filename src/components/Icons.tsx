import {
    Box, Search, Star, DownloadCloud, Cpu, Globe, FlaskConical, ShieldCheck,
    DollarSign, Settings, LayoutGrid, Sparkles, BrainCircuit, Atom, Terminal,
    Activity, Zap, Layers, Command, Code, Send, X, RefreshCw, ChevronRight,
    Database, Network, Infinity, ShoppingCart, User, LogOut, Play, Square,
    Clock, Share2, GitBranch, Rocket, FastForward, Loader, Link, ToggleLeft,
    ToggleRight, CheckCircle2, AlertTriangle, FileCode, ArrowRight, MessageSquare,
    ChevronLeft, ArrowLeftRight, Home, Lightbulb, Eye, Clipboard, Key,
    Fingerprint, Maximize, Minimize, UploadCloud, BarChart, Heart, Wallet,
    Scale, Trophy, Gift, Briefcase, ExternalLink, Cloud, HardDrive, Lock,
    Mic, MicOff, Trash, PenSquare, Brackets, TrendingUp, Truck, Dna, Info, MapPin, Inbox, BookOpen, EyeOff, Menu,
    Server, XCircle, Bug, ChevronUp, ChevronDown, Orbit, ServerCog, Puzzle,
    Warehouse, Users, GraduationCap, Paperclip, File, MessageCircle, Plus, Phone, GitFork, Wifi, Save, Folder, LogIn, Crosshair, Thermometer, CalendarDays,
    Grid, GripVertical, GripHorizontal, ZoomIn, ZoomOut, Move, Ruler, Radio
} from 'lucide-react';

// --- SAFE FALLBACK ---
const Fallback = Box || function FallbackIcon() { return <div style={{width: 24, height: 24, background: 'red'}} />; };

// --- EXPORTS ---
export const BoxIcon = Box || Fallback;
export const SearchIcon = Search || Fallback;
export const StarIcon = Star || Fallback;
export const DownloadCloudIcon = DownloadCloud || Fallback;
export const CpuChipIcon = Cpu || Fallback;
export const GlobeIcon = Globe || Fallback;
export const BeakerIcon = FlaskConical || Fallback;
export const ShieldCheckIcon = ShieldCheck || Fallback;
export const CurrencyDollarIcon = DollarSign || Fallback;
export const ServerCogIcon = ServerCog || Fallback;
export const LayoutGridIcon = LayoutGrid || Fallback;
export const SparklesIcon = Sparkles || Fallback;
export const BrainCircuitIcon = BrainCircuit || Fallback;
export const AtomIcon = Atom || Fallback;
export const TerminalIcon = Terminal || Fallback;
export const ActivityIcon = Activity || Fallback;
export const ZapIcon = Zap || Fallback;
export const LayersIcon = Layers || Fallback;
export const CommandIcon = Command || Fallback;
export const CodeIcon = Code || Fallback;
export const SendIcon = Send || Fallback;
export const XIcon = X || Fallback;
export const RefreshCwIcon = RefreshCw || Fallback;
export const ChevronRightIcon = ChevronRight || Fallback;
export const DatabaseIcon = Database || Fallback;
export const NetworkIcon = Network || Fallback;
export const InfinityIcon = Infinity || Fallback;
export const ShoppingCartIcon = ShoppingCart || Fallback;
export const UserIcon = User || Fallback;
export const LogOutIcon = LogOut || Fallback;
export const PlayIcon = Play || Fallback;
export const StopIcon = Square || Fallback;
export const SettingsIcon = Settings || Fallback;
export const ClockIcon = Clock || Fallback;
export const Share2Icon = Share2 || Fallback;
export const GitBranchIcon = GitBranch || Fallback;
export const RocketLaunchIcon = Rocket || Fallback;
export const FastForwardIcon = FastForward || Fallback;
export const LoaderIcon = Loader || Fallback;
export const LinkIcon = Link || Fallback;
export const ToggleLeftIcon = ToggleLeft || Fallback;
export const ToggleRightIcon = ToggleRight || Fallback;
export const CheckCircle2Icon = CheckCircle2 || Fallback;
export const AlertTriangleIcon = AlertTriangle || Fallback;
export const TimelineIcon = Activity || Fallback; 
export const BugAntIcon = Bug || Fallback;
export const FileCodeIcon = FileCode || Fallback;
export const ArrowRightIcon = ArrowRight || Fallback;
export const MessageSquareIcon = MessageSquare || Fallback;
export const ChevronLeftIcon = ChevronLeft || Fallback;
export const ArrowPathIcon = ArrowLeftRight || Fallback;
export const HomeIcon = Home || Fallback;
export const LightBulbIcon = Lightbulb || Fallback;
export const EyeIcon = Eye || Fallback;
export const ClipboardIcon = Clipboard || Fallback;
export const KeyIcon = Key || Fallback;
export const FingerPrintIcon = Fingerprint || Fallback;
export const CommandLineIcon = Terminal || Fallback;
export const MaximizeIcon = Maximize || Fallback;
export const MinimizeIcon = Minimize || Fallback;
export const UploadCloudIcon = UploadCloud || Fallback;
export const ChartBarIcon = BarChart || Fallback;
export const HeartIcon = Heart || Fallback;
export const BanknotesIcon = DollarSign || Fallback;
export const WalletIcon = Wallet || Fallback;
export const ScaleIcon = Scale || Fallback;
export const TrophyIcon = Trophy || Fallback;
export const GiftIcon = Gift || Fallback;
export const BriefcaseIcon = Briefcase || Fallback;
export const ArrowTopRightOnSquareIcon = ExternalLink || Fallback;
export const CloudServerIcon = Cloud || Fallback;
export const HardDriveIcon = HardDrive || Fallback;
export const LockIcon = Lock || Fallback;
export const MicIcon = Mic || Fallback;
export const MicOffIcon = MicOff || Fallback;
export const TrashIcon = Trash || Fallback;
export const PencilSquareIcon = PenSquare || Fallback;
export const CodeBracketIcon = Brackets || Fallback;
export const MagnifyingGlassIcon = Search || Fallback;
export const ArrowTrendingUpIcon = TrendingUp || Fallback;
export const TruckIcon = Truck || Fallback;
export const DNAIcon = Dna || Fallback;
export const InformationCircleIcon = Info || Fallback;
export const MapPinIcon = MapPin || Fallback;
export const InboxIcon = Inbox || Fallback;
export const BookOpenIcon = BookOpen || Fallback;
export const EyeOffIcon = EyeOff || Fallback;
export const Bars3BottomLeftIcon = Menu || Fallback;
export const ServerStackIcon = Server || Fallback;
export const GalaxyIcon = Orbit || Fallback;
export const DocumentArrowUpIcon = FileCode || Fallback;
export const XCircleIcon = XCircle || Fallback;
export const CodeBracketSquareIcon = Code || Fallback;
export const CubeIcon = Box || Fallback;
export const ChevronUpIcon = ChevronUp || Fallback;
export const ChevronDownIcon = ChevronDown || Fallback;
export const PuzzlePieceIcon = Puzzle || Fallback;
export const BuildingFarmIcon = Warehouse || Fallback;
export const UsersIcon = Users || Fallback;
export const FlaskConicalIcon = FlaskConical || Fallback;
export const AcademicCapIcon = GraduationCap || Fallback;
export const PaperclipIcon = Paperclip || Fallback;
export const FileIcon = File || Fallback;
export const ChatBubbleLeftRightIcon = MessageCircle || Fallback;
export const PlusIcon = Plus || Fallback;
export const PhoneIcon = Phone || Fallback;
export const SourceControlIcon = GitFork || Fallback;
export const WifiIcon = Wifi || Fallback;
export const SaveIcon = Save || Fallback;
export const FolderIcon = Folder || Fallback;
export const LogInIcon = LogIn || Fallback;
export const CrosshairIcon = Crosshair || Fallback;
export const ThermometerIcon = Thermometer || Fallback;
export const CalendarDaysIcon = CalendarDays || Fallback;
export const GridIcon = Grid || Fallback;
export const GripIcon = GripVertical || Fallback;
export const ZoomInIcon = ZoomIn || Fallback;
export const ZoomOutIcon = ZoomOut || Fallback;
export const MoveIcon = Move || Fallback;
export const RulerIcon = Ruler || Fallback;
export const RadioIcon = Radio || Fallback;

// Aliases
export const CloudDownloadIcon = DownloadCloud || Fallback; 
export const CircleStackIcon = Database || Fallback;
export const CuboidIcon = Box || Fallback;
export const CubeTransparentIcon = Box || Fallback;